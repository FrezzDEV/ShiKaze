import type { MangaSourceAdapter } from './manga-source.adapter.js';

/** Register adapters here. Keep source-specific code behind this boundary. */
export function createSourceAdapters(): MangaSourceAdapter[] {
  return [];
}
